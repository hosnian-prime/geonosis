apiVersion: v1
kind: Service
metadata:
  name: {{ include "geonosis.fullname" . }}
  labels:
    {{- include "geonosis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "geonosis.selectorLabels" . | nindent 4 }}
